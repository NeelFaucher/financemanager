import 'package:flutter/material.dart';

class SectionData {
  String categoryInSection;
  double percent;
  Color color;
  double total;

  SectionData(this.categoryInSection, this.percent, this.color, this.total);
}
